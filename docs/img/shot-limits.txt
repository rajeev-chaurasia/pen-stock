$ # the tenant is capped at 3 requests per minute
$ for i in 1 2 3 4 5; do
>   curl -s -o /dev/null -w "request $i: HTTP %{http_code}" ... -D /tmp/h
>   grep -i retry-after /tmp/h
> done
request 1: HTTP 429  Retry-After: 35
request 2: HTTP 429  Retry-After: 35
request 3: HTTP 429  Retry-After: 34
request 4: HTTP 429  Retry-After: 34
request 5: HTTP 429  Retry-After: 34

$ # a rate limit says when to come back. an exhausted budget answers 402
$ # with no Retry-After, because waiting does not refill it.

$ curl -s localhost:9098/metrics | grep denials_total
penstock_denials_total{reason="request_rate",tenant="demo"} 5

$ # cache outcomes are counted too, refusals included
$ curl -s localhost:9098/metrics | grep cache_events_total
penstock_cache_events_total{event="exact_hit"} 2
penstock_cache_events_total{event="ineligible"} 2
penstock_cache_events_total{event="miss"} 2
penstock_cache_events_total{event="stored"} 2

$ # "ineligible" is the two requests that asked sampling to vary. They were
$ # never cached, which is the point: a temperature above zero means the
$ # caller asked for a different answer.
